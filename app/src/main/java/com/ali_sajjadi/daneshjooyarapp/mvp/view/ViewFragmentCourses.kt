package com.ali_sajjadi.daneshjooyarapp.mvp.view

/*
class ViewFragmentCourses(
    private val context: Context
) {

    fun newInstance(arrayList: ArrayList<DataHome>): CoursesFragment{
        val fragment = CoursesFragment(context)
        fragment.arguments = Bundle().apply {
            putStringArrayList(arrayList)
        }
    }

 val binding = FragmentCoursesBinding.inflate(LayoutInflater.from(context))

    val arguments = (context as Fragment).arguments

    val dataList: ArrayList<DataHome>? = arguments?.getParcelableArrayList("dataList")

    fun setRecyclerCourse(){

        binding.coursesRecycler.layoutManager =
            LinearLayoutManager(context, RecyclerView.VERTICAL,false)
        binding.coursesRecycler.adapter = MobileRecyclerAdapter(context,dataList!!)
    }
}*/
